<Window x:Class="Management_Hotel.Views.Dialogs.AddEditRoomTypeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Type de Chambre" Height="706" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#f5f5f5">

    <Border Background="White" 
            Margin="20" 
            CornerRadius="10" 
            Effect="{StaticResource CommonShadow}">

        <StackPanel Margin="30,30,30,21">
            <!-- Header -->
            <TextBlock Text="Détails du type de chambre" 
                     FontSize="24" 
                     FontWeight="SemiBold" 
                     Foreground="#2c3e50"
                     Margin="0,0,0,30"/>

            <!-- Form Fields -->
            <TextBlock Text="Libellé" 
                     Foreground="#34495e"
                     FontSize="14"
                     Margin="0,0,0,8"/>
            <TextBox x:Name="LibelleTextBox" 
                    Margin="0,0,0,20"/>

            <TextBlock Text="Description" 
                     Foreground="#34495e"
                     FontSize="14"
                     Margin="0,0,0,8"/>
            <TextBox x:Name="DescriptionTextBox" 
                    Height="80"
                    TextWrapping="Wrap"
                    AcceptsReturn="True"
                    VerticalContentAlignment="Top"
                    Padding="10"
                    Margin="0,0,0,20"/>

            <TextBlock Text="Capacité" 
                     Foreground="#34495e"
                     FontSize="14"
                     Margin="0,0,0,8"/>
            <TextBox x:Name="CapaciteTextBox" 
                    Margin="0,0,0,20"/>

            <TextBlock Text="Prix par nuit" 
                     Foreground="#34495e"
                     FontSize="14"
                     Margin="0,0,0,8"/>
            <TextBox x:Name="PrixTextBox" 
                    Margin="0,0,0,30"/>

            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" 
                      HorizontalAlignment="Right" 
                      Margin="0,10,0,0">
                <Button Content="Annuler" 
                        Click="CancelButton_Click"
                        Width="120" 
                        Margin="0,0,15,0"/>
                <Button Content="Enregistrer" 
                        Click="SaveButton_Click"
                        Width="120"/>
            </StackPanel>
        </StackPanel>
    </Border>

    <Window.Resources>
        <!-- Button Styles -->
        <Style x:Key="CancelButton" TargetType="Button">
            <Setter Property="Background" Value="#e74c3c"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#c0392b"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ConfirmButton" TargetType="Button">
            <Setter Property="Background" Value="#2ecc71"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#27ae60"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
</Window>
    