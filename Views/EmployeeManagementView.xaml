<UserControl x:Class="Management_Hotel.Views.EmployeeManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="Ajouter un employé" Click="AddEmployee_Click" 
                    Width="150" Height="30" Margin="0,0,10,0"/>
            <Button Content="Actualiser" Click="Refresh_Click" 
                    Width="100" Height="30"/>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="EmployeesGrid" 
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Nom}"/>
                <DataGridTextColumn Header="Prénom" Binding="{Binding Prenom}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}"/>
                <DataGridTextColumn Header="Téléphone" Binding="{Binding Telephone}"/>
                <DataGridTextColumn Header="Rôle" Binding="{Binding Role}"/>
                
                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Modifier" Click="EditEmployee_Click" Margin="0,0,5,0"/>
                                <Button Content="Supprimer" Click="DeleteEmployee_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
